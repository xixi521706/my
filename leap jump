#include <stdio.h>

int jump(int n)
{
    if(n<=1)
       return 1;
    else
    return jump(n-1)+jump(n-2);
}
int main()
{
    int n=0;
    scanf("%d",&n);
    int ret=jump(n);
    printf("%d \n");
    return 0;
}
